import React from 'react';
import DashboardPage from '@/views/vendor-dashboard/booking';

export const metadata = {
  title: 'Vendor History || Efica ',
  description: 'Efica ',
};

export default function page() {
  return <DashboardPage />;
}
