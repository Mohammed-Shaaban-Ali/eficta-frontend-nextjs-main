import React from 'react';
import DashboardPage from '@/views/vendor-dashboard/add-hotel';

export const metadata = {
  title: 'Vendor Add Hotel || Efica ',
  description: 'Efica ',
};

export default function page() {
  return <DashboardPage />;
}
