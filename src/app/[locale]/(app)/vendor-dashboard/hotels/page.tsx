import React from 'react';
import DashboardPage from '@/views/vendor-dashboard/hotels';

export const metadata = {
  title: 'Vendor Hotels || Efica ',
  description: 'Efica ',
};

export default function page() {
  return <DashboardPage />;
}
