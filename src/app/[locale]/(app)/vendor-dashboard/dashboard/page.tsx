import React from 'react';
import DashboardPage from '@/views/vendor-dashboard/dashboard';

export const metadata = {
  title: 'Vendor Dashboard || Efica ',
  description: 'Efica ',
};

export default function page() {
  return <DashboardPage />;
}
