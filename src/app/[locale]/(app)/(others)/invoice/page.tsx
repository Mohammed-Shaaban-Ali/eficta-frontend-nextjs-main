import InvoiceComponent from '@/views/invoice/Invoice';

export const metadata = {
  title: 'Invoice || Efica ',
  description: 'Efica ',
};

const Invoice = () => {
  return (
    <>
      <InvoiceComponent />
    </>
  );
};

export default Invoice;
